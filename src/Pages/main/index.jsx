import React from 'react';
import Layout from '../../Layout';

import './index.scss';

// ========================================
const MainPage = (props)=>{

    return(
        <Layout name="main">
            <main-page>
                <div>
                    sample title in main page
                </div>
            </main-page>
        </Layout>
    )
}

export default MainPage;